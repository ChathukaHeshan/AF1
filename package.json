{
  "name": "backend",
  "version": "1.0.0",
  "description": "Backend of the ICAF project",
  "engines": {
    "node": "16.x"
  },
  "main": "index.js",
  "scripts": {
    "build": "cd client && npm run build",
        "install-client": "cd client && npm install",
        "heroku-postbuild": "npm run install-client && npm run build",
        "start": "nodemon index.js"
    },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "config": "^3.3.6",
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "mongoose": "^5.12.13",
    "multer": "^1.4.2",
    "nodemon": "^2.0.7"
  },
  "devDependencies": {
    "jest": "^27.0.6"
  }
}
